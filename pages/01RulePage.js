/** --------------------Description-------------------
 * ddl = dropdown list
 * lbl = label
 * btn = button
 * cvs = canvas
 * tb = table
 * hd = header
 * ft = footer
 * bd = body
 * dal = dialog
 * lb = list box (ddl->lb->items)
 * tbbd = table body: the body of table
 * tbft = table footer: the footer of table
 * tbhd = table header: the header of table
 * ic= icon
 * tt=tooltip
 * txt= textbox
 */
